[package]
name = "dilian_macros"
version = "1.0.1"
edition = "2024"
description = "Macros used for simplifying the creation of services used for dependency injection"
license = "MIT"

[dependencies]
syn = { version =  "2.0.114", features = ["full"] }
quote = "1.0.44"

[lib]
proc-macro = true
