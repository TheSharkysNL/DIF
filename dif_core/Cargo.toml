[package]
name = "dif_core"
version = "1.4.0"
edition = "2024"

[features]
async = ["tokio"]
test-utils = []

[dependencies]
tokio = { version = "1.53.1", optional = true, features = ["sync"] }
rustc-hash = "2.1.3"
