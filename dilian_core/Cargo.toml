[package]
name = "dilian_core"
version = "1.1.3"
edition = "2024"
description = "The core backend used by the dil crate"
license = "MIT"

[features]
async = ["tokio"]
globals = []

[dependencies]
tokio = { version = "1.53.1", optional = true, features = ["sync"] }
rustc-hash = "2.1.3"
